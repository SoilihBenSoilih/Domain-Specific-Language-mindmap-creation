1. Le dossier << atompm >> contient les fichiers suivante:
   syntaxe abstraite(MindMap_CarteMentale.model), 
   la syntaxe concrète(MindMap_CarteMentale.defaultIcons.model)
   et l'instance dans le dossier "instance".

2. Le dossier << Xtext >> contient le fichier de la grammaire(MyDsl.xtext),
   le métamodèle généré(MyDsl.ecore), l'éditeur textuel(MyDsl.genmodel),
   le fichier qui définit la liste des fragments utilisés pour la  
   génération de l'éditeur(GenerateMyDsl.mwe2) et l'instance dans le 
   le dossier instance.

3. Le dossier << images >> contient les images présents dans le rapport.

4. Le dossier << sources >> contient un article sur lequel on s'est basé 
   pour la création de notre grammaire.

5. Le dossier << Projets complets >> contient les deux projets dans leur 
   intégralité.

6. le fichier << Atompm ou Xtext.pdf >> est la réponse à la question 5. 

